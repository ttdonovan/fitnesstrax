import React from 'react'
import { connect } from 'react-redux'

class Calendar extends React.Component {
    render () {
        return null
    }
}

export const CalendarView = connect(() => ({}), () => ({}))(Calendar)

